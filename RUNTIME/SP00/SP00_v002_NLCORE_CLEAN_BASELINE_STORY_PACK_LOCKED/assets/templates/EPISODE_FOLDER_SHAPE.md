# EPISODE_FOLDER_SHAPE v001

Each episode folder must keep index, ready, v2, layer, layer binding manifest, crosscheck, frozen, execution queue, and sources separate.
Body sources, constraints, process-only files, and reference-only files must not be collapsed into one summary.
