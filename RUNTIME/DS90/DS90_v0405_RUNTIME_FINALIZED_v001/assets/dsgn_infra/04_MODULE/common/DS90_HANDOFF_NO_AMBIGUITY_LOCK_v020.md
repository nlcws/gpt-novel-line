# DS90 HANDOFF NO AMBIGUITY LOCK v020

STATUS: ACTIVE

A downstream individual must not infer the current file, current route, current shelf, current STOP, or current acceptance status.
If any of those is missing, return STOP with the missing item list instead of producing a handoff.
