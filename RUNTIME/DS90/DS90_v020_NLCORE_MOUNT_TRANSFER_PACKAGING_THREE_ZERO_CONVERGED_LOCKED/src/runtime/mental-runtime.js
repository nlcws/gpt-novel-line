/*
GPT MENTAL RUNTIME CONTRACT v020

1. Treat this source tree as executable specification, not prose advice.
2. GPT never runs npm test and never depends on package.json.
3. Read START_HERE, READ_ORDER, ALWAYS_READ, and requiredReads(operation).
4. Unknown, ambiguous, unreadable, or unverifiable conditions STOP.
5. Files being present in the ZIP is not proof of use.
6. User intent mismatch is a STOP, not a soft warning.
7. MOUNT_TRANSFER and PACK_CUTOUT are root routes.
8. Convergence requires three consecutive zero dry-runs.
9. Runtime remains neutral and external context remains external.
*/

export const MENTAL_RUNTIME_VERSION = "DS90-v020-NLCORE-MOUNT-TRANSFER-PACKAGING-THREE-ZERO-LOCKED";
