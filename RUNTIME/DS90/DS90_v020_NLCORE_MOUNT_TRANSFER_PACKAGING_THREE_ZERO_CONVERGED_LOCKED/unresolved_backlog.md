# Unresolved Backlog

Runtime implementation items under the v019 instruction: none.

The following are external acceptance or future explicit-decision items, not missing
v019.6 implementation: reciprocal handoff respect lock added.

v019 implementation:

- Do not reduce the five episode files until real-project context-loss testing is complete.
- Keep supplied layer canon name/version unchanged until an explicit replacement decision exists.
- Repeated episode-only layer findings may enter backlog, but cannot be promoted automatically.

v019.7 implementation: end-user heat delivery lock added. No known unresolved blocker from Node tests.
