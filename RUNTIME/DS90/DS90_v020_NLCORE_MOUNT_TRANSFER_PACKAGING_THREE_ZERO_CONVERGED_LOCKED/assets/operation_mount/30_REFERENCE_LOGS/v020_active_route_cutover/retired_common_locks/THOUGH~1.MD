# THOUGHT_INVARIANT_IMPLEMENTATION_VARIABLE_LOCK v019.14a

STATUS: CURRENT_SPEC
APPLIES_TO_RUNTIME: v019.15-NLCORE-HISTORY-MASTER-REAPPLY-LOCKED

コード、組み方、検査実装は可変である。
不変なのは思想、禁止線、責任境界、検査目的である。

```text
可変:
  code
  folder shape
  schema implementation
  file name
  test implementation
  template body

不変:
  thought
  boundary
  stop purpose
  handoff responsibility
  no-residue policy
  self-contained pack policy
```

通らないコードは変えてよい。
ただし守る思想を変えてはならない。
