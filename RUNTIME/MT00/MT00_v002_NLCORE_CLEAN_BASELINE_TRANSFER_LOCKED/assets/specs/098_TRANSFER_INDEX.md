# 098_TRANSFER_INDEX

The transfer index binds item IDs, entity IDs, relation IDs, stop tags, locations, and shelf IDs.

Parallel indexes are forbidden unless explicitly declared as a different role. Existing index repair comes before new index creation.
